name: Super-Linter

on: workflow_dispatch

jobs:
  super-lint:
    name: Lint code base
    runs-on: self-hosted
    steps:
      - name: Check out code
        uses: actions/checkout@v2
        
      - name: Do stuff?
        run: |
          ssh root@209.151.152.149
          
